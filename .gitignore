# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Node / backend (ignore node_modules at any level, lockfiles, logs and env)
node_modules/
**/node_modules/
package-lock.json
npm-shrinkwrap.json
yarn.lock
pnpm-lock.yaml
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*
.env
.env.*
.env.local
.env.*.local
/coverage/
/nyc_output/
*.key
*.pem
*.p12
*.jks
*.keystore
serviceAccountKey.json
credentials.json
.secrets
.secret
backend/_scripts/



# Android / Gradle and local settings
/android/.gradle/
/.gradle/
/local.properties
/android/**/build/
**/build/

# iOS
**/ios/Pods/
**/ios/Podfile.lock
**/ios/Runner.xcworkspace/
**/ios/Flutter/Generated.xcconfig

# Editors/IDEs
.vscode/

# OS files
Thumbs.db
desktop.ini

# Backend uploads and generated files (optional)
/backend/uploads/
/backend/uploads/generated/

# Google/Service files (ignore if you don't want them in repo)
android/app/google-services.json
ios/GoogleService-Info.plist

# Useful commands to remove files already tracked by git:
# 1) Remove specific file/directory from the index (keeps local copy):
#     git rm -r --cached backend/uploads
# 2) Clear and re-add all (careful, re-adds everything permitted by .gitignore):
#     git rm -r --cached .
#     git add .
#     git commit -m "Apply updated .gitignore"
